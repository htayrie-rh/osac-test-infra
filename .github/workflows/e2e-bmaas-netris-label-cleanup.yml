---
name: Remove BMaaS Netris trigger on new push

on:
  pull_request_target:
    types: [synchronize]

permissions:
  contents: read
  pull-requests: write

concurrency:
  # Use the same PR-scoped group as the label-triggered Netris workflow so a
  # new commit requests cancellation of the previous Netris run immediately.
  group: e2e-bmaas-netris-full-install-${{ github.event.pull_request.head.repo.full_name || github.repository }}-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  reset-trigger:
    name: Reset BMaaS Netris trigger
    if: contains(github.event.pull_request.labels.*.name, 'e2e-bmaas-netris')
    runs-on: ubuntu-latest
    steps:
      - name: Remove stale Netris trigger label
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          set -euo pipefail
          gh pr edit "${PR_NUMBER}" --repo "${REPO}" --remove-label "e2e-bmaas-netris"
          gh pr comment "${PR_NUMBER}" --repo "${REPO}" \
            --body "Removed \`e2e-bmaas-netris\` due to new commits. Re-run \`/test e2e-bmaas-netris-full-install\` for the new revision."
